(program
 (define break (void))
 (define return (void))
 (define continue (void))
 (define $current-handler (void))
 (begin
   (if (if ((lambda (cv14) (begin (if (< 1900 cv14) (< cv14 2100) #f))) g$year)
         (if ((lambda (cv15) (begin (if (<= 1 cv15) (<= cv15 12) #f))) g$month)
           (if ((lambda (cv16) (begin (if (<= 1 cv16) (<= cv16 31) #f))) g$day)
             (if ((lambda (cv17) (begin (if (<= 0 cv17) (< cv17 24) #f)))
                  g$hour)
               (if ((lambda (cv18) (begin (if (<= 0 cv18) (< cv18 60) #f)))
                    g$minute)
                 ((lambda (cv19) (begin (if (<= 0 cv19) (< cv19 60) #f)))
                  g$second)
                 #f)
               #f)
             #f)
           #f)
         #f)
     ((lambda () (begin (return 1))))
     (void))))
