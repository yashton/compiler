(program
 (define break (void))
 (define return (void))
 (define continue (void))
 (define $current-handler (void))
 (begin
   ((lambda (e23)
      (begin
        ((lambda (i22)
           (begin
             (if (py-list? e23)
               (py-list-ref e23 i22)
               (if (tuple? e23)
                 (tuple-ref e23 i22)
                 (if (dict? e23)
                   (dict-ref e23 i22)
                   (error "cannot index object"))))))
         5)))
    ((lambda (e21)
       (begin
         ((lambda (i20)
            (begin
              (if (py-list? e21)
                (py-list-ref e21 i20)
                (if (tuple? e21)
                  (tuple-ref e21 i20)
                  (if (dict? e21)
                    (dict-ref e21 i20)
                    (error "cannot index object"))))))
          4)))
     ((lambda (e19)
        (begin
          ((lambda (i18)
             (begin
               (if (py-list? e19)
                 (py-list-ref e19 i18)
                 (if (tuple? e19)
                   (tuple-ref e19 i18)
                   (if (dict? e19)
                     (dict-ref e19 i18)
                     (error "cannot index object"))))))
           3)))
      ((lambda (e17)
         (begin
           ((lambda (i16)
              (begin
                (if (py-list? e17)
                  (py-list-ref e17 i16)
                  (if (tuple? e17)
                    (tuple-ref e17 i16)
                    (if (dict? e17)
                      (dict-ref e17 i16)
                      (error "cannot index object"))))))
            2)))
       ((lambda (e15)
          (begin
            ((lambda (i14)
               (begin
                 (if (py-list? e15)
                   (py-list-ref e15 i14)
                   (if (tuple? e15)
                     (tuple-ref e15 i14)
                     (if (dict? e15)
                       (dict-ref e15 i14)
                       (error "cannot index object"))))))
             1)))
        g$a)))))))
